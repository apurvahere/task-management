import Layout from "../../components/layout/Layout";
import LoginPage from "./LoginPage";

const Login = () => {
  return (
    <Layout>
      <LoginPage />
    </Layout>
  );
};

export default Login;
